import { ComponentFactoryResolver } from "@angular/core"

export interface Pie{
    name: String,
    description: String,
    price: number,
    stock: number,
    image: String,
    clearance: Boolean,
}